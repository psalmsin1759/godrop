import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Rider app smoke test', (WidgetTester tester) async {
    // Placeholder test — full integration tests require a running backend.
    expect(true, isTrue);
  });
}
